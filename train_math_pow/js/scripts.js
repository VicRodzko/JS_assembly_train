let base, 
    exponent, 
    answer;                                            // quest of variables
base = prompt("Введите желаемое число", " ");          // enter first number
exponent = prompt("Введите желаемую степень", " ");    // enter rank
answer = Math.pow(base, exponent);                     // mathematic function

alert("Ответ: " + answer);                             // withdraw in window (display, monitor)